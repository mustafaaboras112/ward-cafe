# كافيه ورد بلاس — دليل التسليم

تاريخ التجهيز: 2026-09-08

هذه حزمة المصدر الحالية للمراجعة والتسليم الفني، وتشمل الواجهة والخدمات وملفات الفحص. لم يُنفّذ نشر سحابي ضمن هذا التسليم.

## الإضافات الأخيرة

- هوية وردية موحدة للإدارة والكاشير والمطبخ والكارسون والمحاسبة وتسجيل الدخول.
- انتقالات خفيفة للتصنيفات والمنتجات والسلة والنوافذ، مع احترام تقليل الحركة.
- وضع ليلي للموظفين يحفظ الاختيار محليًا ويتزامن بين تبويبات المتصفح.
- شريط متابعة للعميل: استلمنا طلبك، عم نجهّزه، جاهز، تم التوصيل.
- يبقى الطلب المسلم ظاهرًا للعميل حتى الدفع. المراحل مرتبطة بحالة الطلب المسجلة.
- تنسيقات الوضع الليلي وهوية الموظفين محصورة بالشاشة للمحافظة على الطباعة.

## التشغيل والمعاينة

1. فك ضغط الحزمة في مجلد مستقل.
2. قدّم المجلد عبر خادم HTTP محلي. إذا كان Python متوفرًا، افتح الطرفية داخل المجلد وشغّل `python -m http.server 8080 --bind 127.0.0.1`.
3. افتح `http://127.0.0.1:8080/index.html` لمنيو العميل.
4. صفحات الموظفين: `pos.html` للكاشير، `kitchen.html` للمطبخ، `waiter.html` للكارسون، `accounting.html` للمحاسبة، `admin.html` للإدارة، و`login.html` للدخول الرقمي.
5. زر «الوضع الليلي» موجود ضمن رأس الصفحة أو القائمة الجانبية أو بطاقة الدخول حسب الشاشة.

ملف `firebase-config.js` يشير إلى مشروع Firebase الحالي؛ تشغيل نسخة محلية لا يعزل البيانات عن ذلك المشروع. استخدم إعداد مشروع تجريبي عند تجربة إنشاء الطلبات أو تعديل البيانات. الإنترنت مطلوب لتحميل Firebase والمكتبات الخارجية.

## الفحص

- نجح `node tests/ward-experience.cjs`: مراحل الطلب، الحالة غير المعروفة، حفظ الوضع، تعذر التخزين، مزامنة التبويبات وربط الصفحات الست.
- نجح فحص بنية `js/menu.js` و`js/ward-theme.js`.
- نجح فحص ربط ملفات الهوية المشتركة بالصفحات.
- تعذر إكمال اختبار المتصفح الأخير لتسجيل الدخول بسبب عطل في محرّك الرسوم. المعاينة البصرية النهائية للوضع الليلي والجوال ما زالت مطلوبة.
- يوجد فشل معروف سابقًا في `tests/workflow.cjs` عند توقع منع التحصيل بعد إغلاق الصندوق.
- اختبارات ظهور الطلبات القديمة قد تتوقع إخفاء الطلب بعد التسليم؛ الإضافة الجديدة تُبقيه حتى الدفع، ولم تُراجع المجموعة القديمة بالكامل لهذه السياسة.

## قبل الاعتماد التشغيلي

القفل المحلي للصفحات لا يساوي صلاحيات خادم. وفق حالة المشروع الموثقة في `FINAL-HANDOVER.md`، يلزم إكمال ربط المصادقة والصلاحيات ومعالجة قواعد قاعدة البيانات العامة قبل النشر العام، وإصلاح فحص إغلاق الصندوق وتجربة تعدد الأجهزة والطباعة. هذه الحزمة ليست اعتمادًا نهائيًا لأمان النظام أو المحاسبة.

مجلد `functions` مخصص للخدمات الخلفية؛ لا تنشره كملفات عامة. إعداد `firebase.json` الحالي يحتوي إعداد قاعدة البيانات فقط، وليس إعداد نشر كامل للخدمات أو الاستضافة.

## محتويات الحزمة

- الصفحات وملفات `js` و`style` والشعار ورموز الطاولات.
- مجلد `functions` وملفات إعداد Firebase وقواعد قاعدة البيانات.
- مجلد `tests` وأدلة المشروع السابقة للرجوع إليها.

هذا الدليل هو ملحق التسليم الأحدث للإضافات البصرية. نتائج الفحص في الأدلة السابقة تاريخية وليست إعادة اعتماد لهذه النسخة. لم تُضمَّن ملفات Git أو إعدادات المحرر أو النسخ المضغوطة السابقة.
